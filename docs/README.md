# Intelligence Platform — Week 8 (Data Pipeline & CRUD)
This projects is an addition to my week 7 file based system for username and hashed password storing. 
This projects consists of full SQLite database with CRUD operations, CSV Loading and enhanced authentication. 

# Project Features 
Sqlite database with 4 tables.
Users migrated from user.txt.
All CSV data loaded.
Contains CRUD functions for all the databases. 
No SQL injection vulnerability.

# Screenshots
The screenshots are added in the docs folder. 
